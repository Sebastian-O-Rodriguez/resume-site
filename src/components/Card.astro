---
export interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<article class="rounded-xl border border-border bg-card text-card-foreground shadow-sm {className}">
  <slot />
</article>